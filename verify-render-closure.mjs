import fs from 'node:fs';
import {consolidateLegacyRenderAssignments,renderDebtInventory} from './render-source-transform.mjs';
const app=fs.readFileSync('app.js','utf8');
const structural=fs.readFileSync('structural-runtime.js','utf8');
const stateRuntime=fs.readFileSync('state-runtime.js','utf8');
const closure=fs.readFileSync('render-closure-runtime.js','utf8');
const index=fs.readFileSync('index.html','utf8');
const fail=[];const need=(c,m)=>{if(!c)fail.push(m);};
let consolidated=null;
try{consolidated=consolidateLegacyRenderAssignments(app,{expected:12});}catch(e){fail.push(e?.message||String(e));}
const sourceDebt=renderDebtInventory(app);
const bundledDebt=consolidated?renderDebtInventory(consolidated.source):{assignments:-1,declarations:-1,baseAliases:-1,destructiveRootWrites:-1};
need(sourceDebt.assignments===12,`Inventario fuente inesperado: ${sourceDebt.assignments} render=function; se esperaban 12 antes de la sustitución física.`);
need(sourceDebt.declarations===1,`Inventario fuente inesperado: ${sourceDebt.declarations} function render; se esperaba 1.`);
need(sourceDebt.baseAliases===5,`Inventario fuente inesperado: ${sourceDebt.baseAliases} aliases renderV*Base; se esperaban 5.`);
need(bundledDebt.assignments===0,`La consolidación deja ${bundledDebt.assignments} render=function en el app empaquetado.`);
need(bundledDebt.declarations===1,`La consolidación debe conservar exactamente el render bootstrap base; hay ${bundledDebt.declarations}.`);
need(bundledDebt.baseAliases===0,`El bundle conserva ${bundledDebt.baseAliases} aliases renderV*Base muertos.`);
need(bundledDebt.destructiveRootWrites===1,`El bundle debe conservar solo el root write del render bootstrap base; hay ${bundledDebt.destructiveRootWrites}.`);
need(consolidated?.removed===12,`La consolidación eliminó ${consolidated?.removed??0}/12 assignments.`);
need(consolidated?.renderAliasesRemoved===5,`La consolidación eliminó ${consolidated?.renderAliasesRemoved??0}/5 aliases muertos.`);
need(structural.includes('/* Final runtime coordinator. This is the only render() used after bootstrap completes. */'),'Structural Runtime ya no declara el coordinador final esperado.');
need(structural.includes('window.render=render;'),'Structural Runtime no publica su coordinador.');
need(stateRuntime.includes('const trStateRenderBase=render;'),'State Runtime ya no captura el render estructural.');
need(stateRuntime.includes('trDomainRenderGuardBegin()')&&stateRuntime.includes('trDomainRenderGuardEnd()'),'State Runtime perdió el boundary read-only de render.');
need(stateRuntime.includes('window.render=render;'),'State Runtime no publica el render protegido.');
need(closure.includes("const TR_RENDER_CLOSURE_VERSION='31.23.5'"),'Render closure tiene versión inesperada.');
need(closure.includes("const TR_SOURCE_CONSOLIDATION_PHASE='31.23.14'"),'La tarjeta visible no identifica V31.23.14.');
need(closure.includes('State Registry Migration III'),'Falta la etiqueta visible de State Registry Migration III.');
need(closure.includes('function trSourceConsolidationDiagnostics()'),'Falta diagnóstico runtime de Source Consolidation.');
need(closure.includes('function trSourceConsolidationPanel()'),'Falta panel runtime de Source Consolidation.');
for(const meta of ['trading-research-app-global-source-blocks','trading-research-app-global-bundled-blocks','trading-research-app-global-source-entries','trading-research-app-global-bundled-entries','trading-research-app-global-source-unique','trading-research-app-global-bundled-unique','trading-research-state-registry-migrated','trading-research-state-registry-batch1','trading-research-state-registry-batch2','trading-research-state-registry-batch3','trading-research-prune-safe-candidates','trading-research-dynamic-handler-slots','trading-research-dynamic-candidate-roots','trading-research-dynamic-protected-globals','trading-research-contract-map-remaining','trading-research-contract-map-classified','trading-research-contract-map-unclassified','trading-research-contract-map-multi','trading-research-contract-map-primary-state','trading-research-contract-map-primary-handler','trading-research-contract-map-primary-dynamic','trading-research-contract-map-state-frontier','trading-research-contract-map-handler-frontier','trading-research-contract-map-cross-runtime'])need(closure.includes(meta),`Source Consolidation no consume ${meta}.`);
need(closure.includes('sourceBlocks===51&&bundledBlocks===44'),'Source Consolidation no valida el cierre de bloques explícitos.');
need(closure.includes('sourceEntries===375&&bundledEntries===300'),'Source Consolidation no valida la tercera migración de entries State.');
need(closure.includes('sourceUnique===332&&bundledUnique===262'),'Source Consolidation no valida la tercera migración de exports State.');
need(closure.includes('stateMigrated===24&&stateBatch1===8&&stateBatch2===8&&stateBatch3===8'),'Source Consolidation no valida los tres lotes State de 8 acciones.');
need(closure.includes('removedBlocks===7&&removedEntries===75&&removedUnique===70&&safeCandidates===0'),'Source Consolidation no conserva la guardia acumulada.');
need(closure.includes('contractRemaining===262&&contractClassified===262&&contractUnclassified===0&&contractMulti===37'),'Source Consolidation no valida cobertura contractual post-migración III.');
need(closure.includes('primaryState===36&&primaryHandler===223&&primaryDynamic===3'),'Source Consolidation no valida categorías post-migración III.');
need(closure.includes('stateFrontier===32&&handlerFrontier===221&&crossRuntime===0'),'Source Consolidation no valida fronteras post-migración III.');
need(closure.includes('dynamicSlots===4&&dynamicCandidates===8&&dynamicProtected===3'),'Source Consolidation no valida Dynamic Action Guard.');
for(const name of ['setConfigTab','switchPlan','switchPlanAndOpen','togglePlanStatus','saveRiskManagement','saveRiskStrategy','saveEmotionalEditor','toggleGoalActive'])need(closure.includes(name),`Panel V31.23.14 no identifica acción migrada ${name}.`);
need(closure.includes('sourceConsolidation:trSourceConsolidationDiagnostics'),'TradingResearchRender no expone el diagnóstico namespaced de consolidación.');
need(closure.includes('const trRenderClosureBase=window.render;'),'Render closure no captura el entry point final previo.');
need(closure.includes('function trCanonicalRenderEntry('),'Falta el entry point canónico.');
need(closure.includes('window.render=trCanonicalRenderEntry;'),'El entry point canónico no se publica globalmente.');
need(closure.includes('window.TradingResearchRender=TradingResearchRender;'),'Falta API TradingResearchRender.');
need(!closure.includes('Object.assign(window,{trRenderClosureDiagnostics,trRenderClosurePanel})'),'Render Closure vuelve a publicar aliases diagnósticos duplicados.');
need(!closure.includes('Object.assign(window,{trSourceConsolidationDiagnostics,trSourceConsolidationPanel})'),'Source Consolidation no debe añadir aliases diagnósticos globales.');
need(!closure.includes("document.getElementById('app').innerHTML=shell()"),'Render closure no debe volver a introducir un render destructivo propio.');
need(index.includes('<script src="render-closure-runtime.js"></script>'),'index.html no carga render-closure-runtime.js.');
need(index.indexOf('render-closure-runtime.js')>index.indexOf('style-runtime.js'),'render-closure-runtime.js debe cargar el último entre los runtimes locales.');
if(fail.length){console.error('\nCanonical render closure verification FAILED');for(const x of fail)console.error(' - '+x);process.exit(1);}
console.log('Canonical render closure verification OK');
console.log(' - Production entry: Structural Runtime -> State read-only guard -> Render Closure');
console.log(` - Source legacy render assignments: ${sourceDebt.assignments}; bundled: ${bundledDebt.assignments}`);
console.log(` - Source renderV*Base aliases: ${sourceDebt.baseAliases}; bundled: ${bundledDebt.baseAliases}`);
console.log(` - Bundled destructive root writes: ${bundledDebt.destructiveRootWrites}`);
console.log(' - Render diagnostics exposed only through TradingResearchRender');
console.log(' - Visible consolidation phase: V31.23.14 + State Registry Migration III diagnostics');
